export const metadata = {
  title: "Blog | Telegram Channels",
  description:
    "Articles, guides, and tips for Telegram channels and communities.",
};

export default function BlogLayout({ children }) {
  return children;
}
